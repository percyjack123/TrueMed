## ML module for counterfeit medicine detection

The trained deep learning model (`real_fake_model.h5`) is not stored in GitHub
because it exceeds GitHub’s file size limit.

The model is hosted on Hugging Face:

https://huggingface.co/SKumarii/truemed-real-fake-model

The Streamlit app automatically downloads the model at runtime.
